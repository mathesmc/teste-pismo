version: "2"
sql:
  - engine: "postgresql"
    queries: "database/query.sql"
    schema: "database/migration/"
    gen:
      go:
        package: "main"
        emit_json_tags: true
        emit_prepared_queries: true
        emit_interface: true
        out: "./database/sqlc/"
